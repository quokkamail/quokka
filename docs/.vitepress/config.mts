import { defineConfig } from "vitepress";

export default defineConfig({
  title: "Quokka",
  description: "One-Click, Self-Hosted Mail Server",
  themeConfig: {
    nav: [],
    socialLinks: [
      { icon: "github", link: "https://github.com/quokkamail/quokka" },
    ],
  },
});
